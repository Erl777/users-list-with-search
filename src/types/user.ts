export interface User {
    id: number,
    name: string,
    username: string,
    email: string,
    phone: string
}

export interface Ids {
    id: string[]
}

export interface Names {
    name: string[]
}
